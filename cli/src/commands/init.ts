import { Command } from 'commander'

export const initCommand = new Command('init')
  .description('Initialize a new Spry project or configure an existing one')
  .action(async () => {
    // TODO: implement init flow
    console.log('spry init — not yet implemented')
  })
